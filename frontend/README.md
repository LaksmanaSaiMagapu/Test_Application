# GeoNexus GIS Dashboard — Frontend

React + Vite + MapLibre GL 6 dashboard. See the [root README](../README.md) for full setup instructions.

```bash
npm install
npm run dev     # start dev server on http://localhost:5173
npm run build   # production build
npm run lint    # oxlint
```
